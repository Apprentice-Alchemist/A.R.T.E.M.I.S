name: CI

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2  
    # - name: Setup    
    #   uses: krdlab/setup-haxe@master
    #   with:
    #       haxe-version: 4.1.0
    # - name: Haxelibs
    #   run: |
    #     haxelib install hscript --quiet
    #     haxelib install hxnodejs --quiet
    #     haxelib git discordjs https://github.com/Apprentice-Alchemist/Haxe-DiscordJs --quiet
    #     haxelib git ModioHx https://github.com/Apprentice-Alchemist/ModioHx --quiet
    # - name: Build
    #   run: |
    #    haxe build.hxml
    #    haxe --run Build
    - name: Deploy
      uses: akhileshns/heroku-deploy@v3.0.4
      with:
        heroku_api_key: ${{secrets.HEROKU_API_KEY}}
        heroku_app_name: ${{secrets.HEROKU_APP}}
        heroku_email: ${{secrets.HEROKU_EMAIL}}
        appdir: "node"
      #    git push https://heroku:$HEROKU_API_TOKEN@git.heroku.com/$HEROKU_APP_NAME.git git subtree split --prefix=node origin/master:master --force
      # env:
      #   HEROKU_API_TOKEN: ${{ secrets.HEROKU_API_Key }}
      #   HEROKU_APP_NAME: ${{secrets.HEROKU_APP}}
      #   HEROKU_EMAIL: ${{secrets.HEROKU_EMAIL}}
      # if: github.ref == 'refs/heads/master' && job.status == 'success'
      # run: |
      #   git config user.email $HEROKU_EMAIL
      #   git config user.name "Apprentice Alchemist"
      #   git add --all
      #   git commit -m ...
      #   git push --force https://heroku:$HEROKU_API_TOKEN@git.heroku.com/$HEROKU_APP_NAME.git \`git subtree split --prefix=${appdir} ${branch}\`:refs/heads/master
    - name: Notify Discord (success)
      uses: appleboy/discord-action@master
      with:
        webhook_id: ${{ secrets.DISCORD_WEBHOOK_ID }}
        webhook_token: ${{ secrets.DISCORD_WEBHOOK_TOKEN }}
        color: "#00C07F"
        message: "Build and Deploy succeeded!"

    - name: Notify Discord (failure)
      uses: appleboy/discord-action@master
      if: failure()
      with:
        webhook_id: ${{ secrets.DISCORD_WEBHOOK_ID }}
        webhook_token: ${{ secrets.DISCORD_WEBHOOK_TOKEN }}
        color: "#FF6562"
        message: "Build and or Deploy failed."
